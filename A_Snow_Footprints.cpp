#include<bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin >> n;
    int l = 0, r = 0;
    vector<char> ftp;
    for(int i = 0; i < n;i++)
    {
        char c;
        cin >> c;
        ftp.push_back(c);
        
    }
    for (int i = 0; i < n; i++)
    {
        if(ftp[i] == 'L'{
            l = i;
            bre
        })
    }
    
    for(int i = 0; i < ftp.size();i++){
        if(ftp[i] == 'L'){

        }
        else if(ftp[i] == 'R'){

        }
    }
    
}